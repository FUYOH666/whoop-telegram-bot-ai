# RAS Telegram Bot

[English](README_EN.md) | Русский

Личный метроном дня: 6 осознанных пингов для самосовершенствования.

## Быстрый старт

```bash
git clone https://github.com/FUYOH666/RAS-TGbot.git
cd RAS-TGbot
uv sync
cp .env.example .env
# Заполните ваши API ключи
uv run python -m ras_bot.main
```

## Описание

RAS Bot — это не просто бот с напоминаниями. Это личный метроном дня, который помогает поддерживать эталонный ритм через 6 мягких касаний и программирует подсознание на успех и изобилие.

**Философия:** Каждое сообщение использует принципы нейролингвистического программирования (НЛП) — позитивные утверждения в настоящем времени, визуализацию успеха, образы изобилия и больших чеков. Это не просто напоминания, а программа для подсознания на создание финансовой свободы.

**Интеграция WHOOP:** Бот интегрирован с WHOOP для получения физических показателей (Recovery, Sleep, Strain, Workouts) и их использования в сообщениях для более точного программирования подсознания. Физическое состояние учитывается в каждом слоте, что позволяет создавать более персонализированные и актуальные сообщения.

**6 слотов дня:**

1. **S1 — Утро тела** (07:30) — подъём, завтрак, зал/море
2. **S2 — Опора "я есть"** (09:30) — 5–10 минут тишины и практики
3. **S3 — Фокусный квант** (11:00) — 60–90 минут глубокой работы над главным проектом
4. **S4 — Шаг к деньгам** (14:00) — один конкретный шаг к рынку
5. **S5 — Закат и присутствие** (17:30) — море, закат, контакт с миром
6. **S6 — Вечерняя интеграция** (21:00) — честная оценка дня

## Установка

### Требования

- Python 3.12+
- [uv](https://github.com/astral-sh/uv) — менеджер пакетов

### Шаги

1. Клонируйте репозиторий:
```bash
git clone https://github.com/FUYOH666/RAS-TGbot.git
cd RAS-TGbot
```

2. Установите зависимости:
```bash
uv sync
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните `.env` своими данными:
- `TELEGRAM_BOT_TOKEN` — токен вашего Telegram бота (обязательно)
- `OPENROUTER_API_KEY` — ключ API от OpenRouter (обязательно)
- `TELEGRAM_USER_ID` — ваш Telegram user ID (опционально, будет установлен автоматически при первом `/start`)
- `WHOOP_CLIENT_ID` — Client ID от WHOOP Developer Platform (опционально, для интеграции WHOOP)
- `WHOOP_CLIENT_SECRET` — Client Secret от WHOOP Developer Platform (опционально, для интеграции WHOOP)

5. **Настройте приватность OpenRouter** (важно для бесплатных моделей):
   - Откройте https://openrouter.ai/settings/privacy
   - Включите "Enable free endpoints that may train on inputs"
   - Включите "Enable free endpoints that may publish prompts" (опционально)
   - Сохраните настройки

6. При необходимости отредактируйте `config.yaml`:
   - Времена слотов (по умолчанию: 07:30, 09:30, 11:00, 14:00, 17:30, 21:00)
   - Промпты для программирования подсознания
   - Параметры LLM (temperature, max_tokens)

7. Запустите бота:
```bash
uv run python -m ras_bot.main
```

8. Откройте Telegram и отправьте боту команду `/start` для активации

**Важно:** Бот будет отправлять сообщения только пользователю, который отправил команду `/start`. Если вы указали `TELEGRAM_USER_ID` в `.env`, планировщик начнет работать сразу. Иначе user_id будет установлен автоматически при первом `/start`.

## Команды бота

### Основные команды
- `/start` — приветствие и начало работы
- `/stats` — статистика выполнения слотов за 7 и 30 дней
- `/health` — проверка состояния бота и всех компонентов

### Команды WHOOP
- `/whoop_connect` — подключение WHOOP (получить authorization URL)
- `/whoop_code <code>` — ввод authorization code после авторизации в WHOOP
- `/whoop_now` — получить текущие показатели WHOOP (Recovery, Sleep, Strain, Workouts)
- `/whoop_monitoring on/off` — включить/выключить автоматический мониторинг стресса
- `/whoop_threshold <value>` — установить порог стресса для уведомлений (0-21, по умолчанию 12.0)
- `/whoop_alerts` — история последних уведомлений о высоком стрессе

## Архитектура

- **aiogram** — Telegram Bot API фреймворк
- **APScheduler** — планировщик задач для отправки слотов
- **OpenRouter + DeepSeek V3.1 (free)** — интеграция с LLM для персонализированных сообщений с программированием подсознания
- **SQLite** — хранение статистики ответов
- **pydantic-settings** — управление конфигурацией

## Особенности

- **Программирование подсознания:** Все сообщения используют принципы НЛП — утверждения в настоящем времени, визуализацию успеха, образы изобилия
- **Персонализация:** Сообщения адаптируются на основе вашей статистики выполнения слотов
- **Интеграция WHOOP:** Физические показатели интегрированы во все слоты для максимальной актуальности:
  - **S1 (Утро тела):** Recovery вчера — для оценки готовности к утру и связи восстановления с созданием изобилия
  - **S2 (Опора "я есть"):** Sleep вчера — для оценки качества восстановления и ясности ума
  - **S3-S5 (Дневные слоты):** Strain сегодня — для оценки текущей нагрузки и баланса энергии
  - **S6 (Вечерняя интеграция):** Все показатели за день — для комплексного аудита и рефлексии
- **Развернутые сообщения:** Увеличенное количество токенов (700 для S1-S5, 1500 для S6) для более глубокого программирования подсознания
- **Fallback:** Если OpenRouter недоступен, используются предзаготовленные сообщения из `config.yaml`
- **Статистика:** Отслеживание выполнения слотов и эталонных дней через команду `/stats`
- **Бесплатная модель:** Используется DeepSeek V3.1 (free) — полностью бесплатная модель с хорошим качеством

## Настройка промптов

Все промпты находятся в `config.yaml`:
- **Системный промпт** — определяет стиль и принципы программирования подсознания
- **Промпты для каждого слота** — инструкции для генерации персонализированных сообщений
- **Fallback сообщения** — используются при недоступности OpenRouter API

Вы можете редактировать промпты под свои цели и стиль общения.

## Интеграция WHOOP

RAS Bot поддерживает интеграцию с WHOOP для получения физических показателей (Recovery, Sleep, Strain, Workouts) и включения их в вечерний слот S6.

### Настройка WHOOP

1. **Создайте приложение в WHOOP Developer Platform:**
   - Зарегистрируйтесь на https://developer.whoop.com/
   - Создайте новое приложение
   - Получите `Client ID` и `Client Secret`
   - Укажите Redirect URL: `https://scanovich.ai/whoop-callback.html` (будет настроен в шаге 4)
   - Разместите Privacy Policy на вашем сайте (например, `scanovich.ai/privacy`)

2. **Добавьте учетные данные в `.env`:**
   ```bash
   WHOOP_CLIENT_ID=your_client_id_here
   WHOOP_CLIENT_SECRET=your_client_secret_here
   ```

3. **Разместите callback страницу на вашем сайте:**
   - Загрузите файл `whoop_callback.html` на ваш сайт `scanovich.ai`
   - Разместите его по адресу: `https://scanovich.ai/whoop-callback.html`
   - Это простая HTML страница, которая обработает OAuth callback от WHOOP

4. **Обновите Redirect URL в WHOOP Developer Platform:**
   - Откройте настройки вашего приложения в WHOOP Developer Platform
   - Измените Redirect URL на: `https://scanovich.ai/whoop-callback.html`
   - Сохраните изменения

5. **Подключите WHOOP в боте:**
   - Отправьте команду `/whoop_connect` боту
   - Перейдите по ссылке и авторизуйтесь в WHOOP
   - После авторизации вы будете перенаправлены на страницу `scanovich.ai/whoop-callback.html`
   - На странице будет показан authorization code - скопируйте его
   - Отправьте команду `/whoop_code <ваш_code>` боту

**Примечание:** Если токен истек и требуется переподключение, просто выполните шаги 5 заново. Бот автоматически сообщит, если потребуется переподключение.

### Как это работает

- **Автоматическое обновление:** Данные WHOOP обновляются ежедневно в 22:00 (после слота S6)
- **Интеграция в S6:** В вечернем слоте S6 перед основным сообщением отображается блок с физическими показателями:
  ```
  📊 Физические показатели WHOOP:
  Recovery: 85% | Sleep: 7.5ч | Strain: 12.5 | Workouts: 2
  ```
- **В промпте LLM:** Данные WHOOP также включаются в промпт для генерации сообщения, чтобы LLM мог учитывать физическое состояние при оценке дня
- **Автоматический refresh токенов:** Токены доступа автоматически обновляются при истечении через refresh_token. Система проверяет токены перед каждым запросом и обновляет их за 5 минут до истечения
- **Долгоживущая сессия:** При правильной настройке refresh_token сессия может работать месяцами без переподключения
- **Обработка rate limits:** Код автоматически обрабатывает ограничения API (10000 запросов/день, 100 запросов/минуту) с retry логикой и экспоненциальной задержкой

**Управление токенами:**
- Токены автоматически проверяются и обновляются перед каждым запросом к WHOOP API
- Если refresh_token отсутствует, система пытается использовать существующий токен (WHOOP может продлевать токены автоматически)
- При истечении токена и отсутствии refresh_token бот автоматически сообщит о необходимости переподключения через `/whoop_connect`

**Мониторинг стресса:**
- Автоматический мониторинг включен по умолчанию после подключения WHOOP
- Проверка Strain каждые 30 минут с 8:00 до 00:00
- Порог стресса настраивается индивидуально через `/whoop_threshold` (по умолчанию 12.0)
- Уведомления учитывают Recovery для более умных рекомендаций
- Можно отключить через `/whoop_monitoring off` и включить обратно через `/whoop_monitoring on`

### Rate Limits WHOOP API

WHOOP API имеет следующие ограничения:
- **10000 запросов в день** — более чем достаточно для ежедневного обновления данных
- **100 запросов в минуту** — достаточно для всех операций бота

**Использование в RAS Bot:**
- Ежедневно выполняется **4 запроса** (Recovery, Sleep, Strain/Workouts, агрегация) в 22:00
- При необходимости обновления токена — **1 дополнительный запрос**
- **Итого: ~5 запросов в день** — это всего **0.05% от дневного лимита**

Код автоматически обрабатывает ошибки rate limit (429) с retry и ожиданием согласно заголовку `Retry-After`.

**Примечание:** Интеграция WHOOP опциональна. Бот работает нормально и без неё. Если WHOOP не настроен или не подключен, слот S6 работает как обычно без физических показателей.

## Production Ready

Проект готов к production использованию:

- ✅ Автоматическое управление токенами WHOOP (refresh перед истечением)
- ✅ Обработка ошибок и retry логика для всех внешних API
- ✅ Graceful shutdown при остановке бота
- ✅ Структурированное логирование (JSON формат)
- ✅ Health check endpoint (`/health`)
- ✅ Docker и Systemd конфигурации для деплоя
- ✅ Полная документация в README.md и DEPLOY.md

### Мониторинг

Бот логирует все важные события в структурированном формате:
- Успешные запросы к API
- Ошибки с деталями
- Обновление токенов
- Отправка сообщений

Для просмотра логов:
```bash
# Docker
docker-compose logs -f

# Systemd
journalctl -u ras-bot -f
```

## Разработка

### Запуск тестов

```bash
uv run pytest
```

### Линтинг

```bash
uv run ruff check .
uv run ruff format .
```

### Типизация

```bash
uv run pyright
```

## Деплой на сервер

Для запуска бота на сервере (чтобы работал 24/7 даже при выключенном ноутбуке) см. [DEPLOY.md](DEPLOY.md).

Кратко:
- **Docker** (рекомендуется): `docker-compose up -d`
- **Systemd**: создайте service файл для автозапуска
- **Screen/Tmux**: для быстрого тестирования

## Вклад в проект

Мы открыты к любым идеям и улучшениям! Если у вас есть предложения или вы хотите внести вклад в проект, пожалуйста:

- Откройте [Issue](https://github.com/FUYOH666/RAS-TGbot/issues) с описанием идеи
- Создайте [Pull Request](https://github.com/FUYOH666/RAS-TGbot/pulls) с реализацией
- Обсудите идеи в [Discussions](https://github.com/FUYOH666/RAS-TGbot/discussions)

Подробнее о том, как внести вклад, см. [CONTRIBUTING.md](CONTRIBUTING.md).

## Автор

Aleksandr Mordvinov

## Благодарности

- Создано с использованием [aiogram](https://github.com/aiogram/aiogram) - современный фреймворк для Telegram ботов
- Использует [OpenRouter](https://openrouter.ai/) для доступа к LLM
- Интегрируется с [WHOOP](https://www.whoop.com/) для метрик здоровья
- Работает на [DeepSeek V3.1](https://www.deepseek.com/) - бесплатная и мощная LLM

---

**Примечание:** Этот бот предназначен для личного использования и самосовершенствования. Он не является медицинским устройством и не должен использоваться как замена профессиональной медицинской консультации.

## Лицензия

MIT License - см. [LICENSE](LICENSE) для деталей.

