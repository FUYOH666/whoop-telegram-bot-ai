version: '3.8'

services:
  whoop-telegram-bot-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: whoop-telegram-bot-ai
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Монтируем БД для персистентности
      - ./whoop_telegram_bot_ai.db:/app/whoop_telegram_bot_ai.db
      # Монтируем конфиг для возможности изменений без пересборки
      - ./config.yaml:/app/config.yaml:ro
      # Монтируем исходный код для возможности изменений без пересборки
      - ./src:/app/src
    environment:
      - PYTHONUNBUFFERED=1
    # Healthcheck для мониторинга
    healthcheck:
      test: ["CMD", "python", "-c", "import sqlite3; sqlite3.connect('/app/whoop_telegram_bot_ai.db')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    # Логирование
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

